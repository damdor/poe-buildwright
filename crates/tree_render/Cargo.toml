[package]
name = "tree_render"
version = "0.1.0"
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true
description = "Render the PoE2 passive tree as a static HTML+SVG file, highlighting selected nodes."

[lints]
workspace = true

[dependencies]
# Intentionally empty: std-only.

[[bin]]
name = "tree_render"
path = "src/main.rs"
